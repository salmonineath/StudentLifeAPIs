services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: studentLife_apis_prod
    restart: always
        env_file:
      - .env
    expose:
      - "5000"
    deploy:
      resources:
        limits:
          memory: 1g
